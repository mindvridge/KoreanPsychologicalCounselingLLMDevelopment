# Core Deep Learning Framework
torch>=2.6.0  # CVE-2025-32434 보안 취약점 해결을 위해 v2.6 이상 필요
transformers>=4.35.0
accelerate>=0.24.0
bitsandbytes>=0.41.0  # 4-bit quantization support
peft>=0.7.0  # LoRA/QLoRA fine-tuning
scipy>=1.11.0  # Statistical analysis for A/B testing
datasets>=2.15.0  # Hugging Face datasets for training
safetensors>=0.4.0  # 안전한 모델 로딩을 위한 safetensors 지원

# Web Interface
gradio>=4.0.0

# RAG and Vector Database
langchain>=0.1.0
chromadb>=0.4.0
sentence-transformers>=2.2.0

# Korean Language Processing
kobert-tokenizer>=0.2.0
konlpy>=0.6.0

# Data Processing
pandas>=2.0.0
numpy>=1.24.0,<2.0.0  # MediaPipe 호환성을 위해 NumPy 1.x 사용
Pillow>=10.0.0  # Image processing
mediapipe>=0.10.0  # 얼굴 랜드마크 감지 및 정확한 감정 분석

# Configuration and Environment
pyyaml>=6.0
python-dotenv>=1.0.0

# Logging and Monitoring
loguru>=0.7.0
cryptography>=41.0.0  # Encryption for privacy-compliant logging

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0  # Coverage reporting
pytest-mock>=3.12.0  # Mocking support
httpx>=0.25.0  # HTTP client for FastAPI testing
psutil>=5.9.0  # System monitoring for performance tests

# API and Web Framework
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0
slowapi>=0.1.9  # Rate limiting
prometheus-client>=0.19.0  # Metrics
python-multipart>=0.0.6  # Form data support

# Database (Long-term Memory)
sqlalchemy>=2.0.0  # ORM for database
alembic>=1.12.0  # Database migrations (optional)

# Additional Utilities
tqdm>=4.66.0
requests>=2.31.0

# =================================
# Voice Counseling (음성 상담)
# =================================

# STT - Speech to Text
openai-whisper>=20231117  # OpenAI Whisper
faster-whisper>=1.0.0  # Faster Whisper (CTranslate2 기반, 더 빠름)
webrtcvad>=2.0.10  # Voice Activity Detection

# TTS - Text to Speech
# Zonos TTS (설치: pip install git+https://github.com/Zyphra/Zonos.git)
# zonos>=0.1.0  # Zonos TTS (별도 설치 필요)
TTS>=0.22.0  # Coqui TTS (대체/보조용)
gTTS>=2.5.0  # Google TTS (폴백용)
elevenlabs>=1.0.0  # ElevenLabs TTS (고품질 음성 합성)

# Audio Processing
sounddevice>=0.4.6  # 실시간 오디오 입출력
soundfile>=0.12.1  # 오디오 파일 읽기/쓰기
librosa>=0.10.1  # 오디오 분석 및 처리
audioread>=3.0.0  # 다양한 오디오 포맷 지원

# WebSocket for Streaming
websockets>=12.0  # WebSocket 클라이언트/서버
aiofiles>=23.2.0  # 비동기 파일 처리

# =================================
# OpenAI API (테스트용)
# =================================
openai>=1.0.0  # ChatGPT API
